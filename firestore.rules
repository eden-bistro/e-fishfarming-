rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // This application does not currently use Cloud Firestore for app data.
    // Deny all client access by default so enabling Firestore cannot expose data.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
